# Установка и запуск Telegram бота

## Решение проблемы с ES модулями

Создан файл `telegram-bot.cjs` в формате CommonJS, который работает независимо от настроек `package.json`.

## Быстрый старт

### 1. Установите зависимости

Выполните в терминале:
```bash
npm install
```

Это установит все зависимости, включая `node-telegram-bot-api`.

### 2. Проверьте установку

Убедитесь, что пакет установлен:
```bash
npm list node-telegram-bot-api
```

### 3. Запустите бота

```bash
npm run bot
```

Или напрямую:
```bash
node telegram-bot.cjs
```

**Примечание:** Используется файл `telegram-bot.cjs` (CommonJS), который работает независимо от настроек ES модулей в `package.json`.

## Если пакет не устанавливается

### Вариант 1: Установка вручную
```bash
npm install node-telegram-bot-api --save
```

### Вариант 2: Очистка и переустановка
```bash
rm -rf node_modules package-lock.json
npm install
```

### Вариант 3: Использование yarn
```bash
yarn add node-telegram-bot-api
yarn bot
```

## Проверка работы

После запуска вы должны увидеть:
```
🤖 TON Wallet Bot запущен!
📱 Web App URL: https://tonvallet.netlify.app
Ожидание сообщений...
```

## Настройка переменных окружения

Создайте файл `.env`:
```env
BOT_TOKEN=ваш_токен_от_BotFather
WEB_APP_URL=https://tonvallet.netlify.app
```

Или установите переменные перед запуском:
```bash
BOT_TOKEN=ваш_токен WEB_APP_URL=https://tonvallet.netlify.app npm run bot
```

## Важно

⚠️ **Не коммитьте токен бота в Git!**
- Токен уже указан в коде (для тестирования)
- Для продакшена используйте переменные окружения
- Добавьте `.env` в `.gitignore` (уже добавлен)
