# Исправление проблемы с поиском NFT

## Проблема
Приложение не находит NFT на кошельке.

## Исправления

### 1. Добавлен прямой запрос к TON API v2 (PRIMARY METHOD)
**Файл:** `src/blockchain/ton.ts`

**Изменения:**
- Добавлен прямой запрос к `https://tonapi.io/v2/accounts/{address}/nfts`
- Этот метод теперь выполняется **первым** (до анализа транзакций)
- Если API возвращает NFT, они сразу возвращаются (без проверки транзакций)

**Преимущества:**
- Быстрее (один запрос вместо анализа транзакций)
- Надежнее (использует официальный API)
- Меньше нагрузка на API (один запрос вместо множества)

### 2. Улучшена обработка разных форматов ответа API
Добавлена поддержка различных форматов ответа:
- `data.nft_items` - основной формат
- `data.items` - альтернативный формат
- `data.nfts` - еще один вариант
- `data` (массив) - прямой массив NFT

### 3. Улучшена обработка метаданных NFT
- Автоматическое извлечение имени, описания, изображения
- Поддержка poster и thumbnail для видео NFT
- Правильная обработка IPFS URL

## Как это работает

### Порядок проверки:
1. **TON API v2** (PRIMARY) - прямой запрос к `tonapi.io/v2/accounts/{address}/nfts`
   - Если успешно и найдены NFT → возвращаем сразу
   - Если ошибка → переходим к следующему методу

2. **Анализ транзакций** (FALLBACK) - если API не сработал
   - Анализ последних транзакций
   - Поиск NFT по паттернам в транзакциях

3. **Паттерн-матчинг** (FALLBACK) - если транзакции не помогли
   - Поиск NFT по известным паттернам

## Проверка работы

### В консоли браузера вы должны увидеть:
```
🔍 Starting NFT detection for address: [ваш адрес]
🔍 API address (raw): [raw адрес]
🔍 Trying TON API v2 for direct NFT query (PRIMARY METHOD)...
📡 Requesting NFTs from TON API: https://tonapi.io/v2/accounts/[address]/nfts
📦 TON API response: [данные]
✅ Found X NFTs from TON API
  ✅ Added NFT: [имя] ([адрес])
✅ Successfully loaded X NFTs from TON API
```

### Если NFT не найдены:
1. Проверьте консоль на наличие ошибок
2. Убедитесь, что адрес кошелька правильный
3. Проверьте, что на кошельке действительно есть NFT
4. Попробуйте обновить страницу

## Возможные проблемы

### Проблема: "Rate limit on TON API"
**Решение:** 
- API имеет ограничения по количеству запросов
- Подождите несколько минут и попробуйте снова
- Приложение автоматически переключится на анализ транзакций

### Проблема: "TON API response format not recognized"
**Решение:**
- API может вернуть данные в неожиданном формате
- Проверьте консоль для детальной информации
- Приложение попробует альтернативные методы

### Проблема: NFT не отображаются
**Решение:**
1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь, что NFT действительно на вашем кошельке
3. Попробуйте обновить страницу (кнопка обновления в NFT коллекции)
4. Проверьте, что адрес кошелька правильный

## Технические детали

### API Endpoint
```
GET https://tonapi.io/v2/accounts/{address}/nfts
```

### Формат ответа (ожидаемый)
```json
{
  "nft_items": [
    {
      "address": "0:...",
      "index": 123,
      "collection": {
        "address": "0:..."
      },
      "metadata": {
        "name": "NFT Name",
        "description": "NFT Description",
        "image": "ipfs://...",
        "poster": "ipfs://...",
        "thumbnail": "ipfs://..."
      }
    }
  ]
}
```

### Обработка ошибок
- Rate limit (429) → переключение на fallback методы
- Другие ошибки → логирование и переключение на fallback
- Пустой ответ → переключение на fallback методы

## Результат

Теперь приложение должно находить NFT на кошельке используя:
1. Прямой запрос к TON API (быстро и надежно)
2. Анализ транзакций (если API не доступен)
3. Паттерн-матчинг (если транзакции не помогли)

Попробуйте открыть NFT коллекцию - NFT должны загрузиться автоматически.
