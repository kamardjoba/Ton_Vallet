# Настройка Telegram бота для TON Wallet

## Шаг 1: Создание бота в Telegram

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "TON Wallet")
   - Введите username бота (должен заканчиваться на `bot`, например: `tonwallet_bot`)
4. BotFather отправит вам **токен бота** - сохраните его!

## Шаг 2: Установка зависимостей

```bash
# Установите зависимости для бота
npm install node-telegram-bot-api

# Или если используете отдельный package.json для бота
npm install --prefix . node-telegram-bot-api
```

## Шаг 3: Настройка переменных окружения

1. Создайте файл `.env` в корне проекта:
```bash
cp .env.example .env
```

2. Откройте `.env` и заполните:
```env
BOT_TOKEN=ваш_токен_от_BotFather
WEB_APP_URL=https://tonvallet.netlify.app
```

## Шаг 4: Настройка Web App в BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newapp`
3. Выберите вашего бота
4. Следуйте инструкциям:
   - Введите название приложения (например: "TON Wallet")
   - Загрузите иконку (опционально)
   - Введите описание
   - Введите URL вашего веб-приложения (например: `https://tonvallet.netlify.app`)
   - Добавьте короткое имя (например: `wallet`)

## Шаг 5: Запуск бота

### Локально (для тестирования):

```bash
# Установите зависимости
npm install

# Запустите бота
npm run bot
# или
node telegram-bot.cjs
```

**Примечание:** Используется файл `telegram-bot.cjs` (CommonJS формат), который работает независимо от настроек ES модулей.

Или с использованием переменных окружения:
```bash
BOT_TOKEN=ваш_токен WEB_APP_URL=https://tonvallet.netlify.app node telegram-bot.js
```

### На сервере (для продакшена):

#### Вариант 1: Использование PM2
```bash
npm install -g pm2
pm2 start telegram-bot.js --name "ton-wallet-bot"
pm2 save
pm2 startup
```

#### Вариант 2: Использование systemd (Linux)
Создайте файл `/etc/systemd/system/ton-wallet-bot.service`:
```ini
[Unit]
Description=TON Wallet Telegram Bot
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/Wallet_Ton_Uni
Environment="BOT_TOKEN=your_token_here"
Environment="WEB_APP_URL=https://tonvallet.netlify.app"
ExecStart=/usr/bin/node /path/to/Wallet_Ton_Uni/telegram-bot.js
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable ton-wallet-bot
sudo systemctl start ton-wallet-bot
```

#### Вариант 3: Использование Heroku/Railway/Render

1. Создайте аккаунт на одном из сервисов
2. Подключите ваш репозиторий
3. Установите переменные окружения:
   - `BOT_TOKEN` - токен от BotFather
   - `WEB_APP_URL` - URL вашего веб-приложения
4. Запустите деплой

## Шаг 6: Проверка работы

1. Найдите вашего бота в Telegram (по username, который вы указали)
2. Отправьте команду `/start`
3. Бот должен ответить приветствием и показать кнопку "Открыть TON Wallet"
4. Нажмите на кнопку - должно открыться веб-приложение

## Команды бота

- `/start` - Начать работу с ботом, показать приветствие и кнопку
- `/help` - Показать справку

## Кнопки

- **🚀 Открыть TON Wallet** - Открывает веб-приложение
- **📖 Помощь** - Показывает справку по использованию
- **ℹ️ О кошельке** - Показывает информацию о кошельке

## Безопасность

⚠️ **Важно:**
- Никогда не публикуйте токен бота в публичных репозиториях
- Используйте переменные окружения для хранения токена
- Добавьте `.env` в `.gitignore`
- Регулярно проверяйте логи на наличие подозрительной активности

## Troubleshooting

### Бот не отвечает
- Проверьте, что токен правильный
- Убедитесь, что бот запущен
- Проверьте логи на наличие ошибок

### Кнопка не открывает приложение
- Проверьте, что `WEB_APP_URL` правильный
- Убедитесь, что веб-приложение доступно по этому URL
- Проверьте, что Web App настроен в BotFather

### Ошибка "Bot token is empty"
- Убедитесь, что переменная окружения `BOT_TOKEN` установлена
- Проверьте файл `.env`

## Дополнительные настройки

### Изменение текста приветствия

Отредактируйте переменную `welcomeMessage` в файле `telegram-bot.js`

### Добавление новых команд

Добавьте обработчики в `telegram-bot.js`:
```javascript
bot.onText(/\/yourcommand/, (msg) => {
  const chatId = msg.chat.id;
  bot.sendMessage(chatId, 'Ваш ответ');
});
```

### Использование Web App без Mini App

Если вы хотите открывать приложение в обычном браузере (не в Telegram Mini App), используйте обычную URL кнопку:

```javascript
{
  text: '🚀 Открыть TON Wallet',
  url: WEB_APP_URL
}
```

Но лучше использовать `web_app`, так как это позволяет открыть приложение прямо в Telegram.
